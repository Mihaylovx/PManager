
name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "build"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "test"

  sonarqube-check:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - run: echo "sonarqube"
